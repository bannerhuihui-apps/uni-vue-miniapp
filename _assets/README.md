# 开发资源（不参与小程序打包）

本目录存放已从 `static/` 迁出的本地素材，运行时请使用火山 TOS 远程地址（见 `src/config/static-images-manifest.json`）。

| 目录 | 说明 |
|------|------|
| `static-images/` | 原 `static/images/`，共 33 张图；换图后可再用 qy 的 TOS 上传测试批量同步 |

上传测试默认路径：`_assets/static-images/`（相对 uni-vue-miniapp 根目录）。
